import styled from "styled-components";

export const Container = styled.div`
  height: 100%;

  img {
    cursor: pointer;
    height: 44px;
    position: absolute;
    display: block;
    bottom: 18px;
    left: 50%;
    transform: translateX(-50%);
  }
`;
